{
  "name": "digichat-admin",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "test:command-guard": "node tools/test-command-guard.mjs",
    "build": "npm run test:command-guard && tsc --noEmit && vite build",
    "build:public": "npm run test:command-guard && tsc --noEmit && vite build --mode public",
    "preview": "vite preview"
  },
  "dependencies": {
    "@microsoft/signalr": "8.0.29"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "vite": "6.4.3"
  }
}
